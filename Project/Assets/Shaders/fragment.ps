#version 330 core
in vec2 texCoords;
out vec4 color;

//uniform sampler2D tex0;
uniform vec3 spriteColor;

void main()
{
	//color = vec4(spriteColor, 1.0) * texture(tex0, texCoords);
	color = vec4(1.0, 1.0, 1.0, 1.0);
}